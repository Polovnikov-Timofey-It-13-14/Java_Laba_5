# Java_Laba_5

Выполнил Половников Тимофей

В лабораторной работе реализовано выполнение всех 8 заданий. Для их выполнения создано:

1. Класс main с вызовом всех задач и пользовательским интерфейсом

2. Fraction, FractionCache и FractionInterface для заданий с дробями

3. Cat, Meowable, CatMeow и CountMeow для подсчета мяуканий

4. Класс ListRemover для удаления строки

5. Класс Olympiad и Participant для вычисления победителя олимпиады

6. Класс QueueEquals для поиска одинаковых элементов очереди

7. Point, Line, PolyLine и PointSort для задачи с точками

8. Класс StreamSort для сортировки строки с именами и номерами

9. Класс Valid для проверки входных данных

Задачи и их решения:

***
1 задание (Задача 1)

В класс Дробь, добавить интерфейс на два метода: получение вещественного значения, установка числителя и установка знаменателя. Сгенерировать такую версию дроби, которая будет кэшировать вычисление вещественного значения. Если раннее в вашем варианте не было Дроби, то создайте сущность Дробь со следующими особенностями:

• Имеет числитель: целое число

• Имеет знаменатель: целое число

• Дробь может быть создана с указанием числителя и знаменателя

• Может вернуть строковое представление вида “числитель/знаменатель”

• Необходимо корректно обрабатывать отрицательные значения. Учтите, что знаменатель не может быть отрицательным.

• Переопределите метод сравнения объектов по состоянию таким образом, чтобы две дроби считались одинаковыми тогда, когда у них одинаковые значения числителя и знаменателя.

Для решения создается класс Fraction, класс FractionCache и интерфейс FractionInterface.

В классе Fraction

- 2 поля (Числитель и знаменатель)

- 2 конструктора

- Геттеры и сеттеры для каждого из полей

- Метод для получение вещественного значения дроби

- Методы для арифметических операций (Сложения, вычитания, умножения и деления)

- toString для вывода дроби строкой

- Методы equals и hashCode для сравнения дробей

Класс FractionCache имеет:

- Поле кэш (в виде map)

- Метод который вычисляет десятичное значение дроби и оставляет его с map

Интерфейс FractionInterface:

- Метод для получения десятичного значения дроби

- Сеттеры для числителя и знаменателя

Решение задачи:

- Создание дроби

- Получение вещественного значения

- Происходит изменение

- Выводится значение измененной дроби и дроби из кэша

Вывод:

1 Задание

Введите числитель: 1

Введите знаменатель: 2

Создана дробь: 1/2

Вызов getDecimalValue(): 0.5

Изменение дроби:

Введите новый числитель: 3

Дробь после изменения: 3/2

Вызов getDecimalValue() после изменения: 1.5

Демонстрация кэширования двух дробей:

Старая дробь 1/2: 0.5

Новая дробь 3/2: 1.5

***

2 задание (Задача 1)

Количество мяуканий.
Необходимо воспользоваться классом Кот и методом принимающим всех мяукающих из задачи 2.5.4. Необходимо таким образом передать кота в указанный метод, что бы после окончания его работы узнать сколько раз мяукал кот за время его работы. На рисунке показан пример работы. Перед вызовом метода создаем кота, отправляем ссылку на кота в метод, после окончания его работы выводим количество мяуканий на экран. Кота изменять нельзя. Если раннее в вашем варианте не было Кота, то создайте

1. сущность Кот, которая описывается следующим образом:
   
• Имеет Имя (строка)

• Для создания необходимо указать имя кота.

• Может быть приведен к текстовой форме вида: “кот: Имя”

• Может помяукать, что приводит к выводу на экран следующего текста: “Имя: мяу!”,

вызвать мяуканье можно без параметров.

3. интерфейс Мяуканье: разработайте метод, который принимает набор объектов способных мяукать и вызывает мяуканье у каждого объекта. Мяукающие объекты должны иметь метод со следующей сигнатурой:

public void meow();

Для решение задачи создается класс Cat, интерфейс Meowable, класс CatMeow и класс CountMeow.

Класс Cat содержит:

- Поле с именем кота

- Конструктор

- Мяуканье кота

- toString для вывода

Интерфейс Meowable содержит:

- Метод для мяуканья

Класс CatMeow:

- Метод, благодаря которому кот мяукает

Класс CountMeow:

- 2 поля (кот и количество мяуканий кота)

- Конструктор

- Метод для подсчета мяуканий (Декоратор)

- Геттер для мяуканий

- toString для вывода

Решение задания:

- Создается кот

- Оборачиваем его в декоратор

- Вызываем метод для мяуканий

- Считаем их без изменения начального класса

- Выводим

Вывод:

2 Задание

Введите имя кота: Барсик

Создан: кот: Барсик

Вызываем метод:

Барсик: мяу!

Барсик: мяу!

Барсик: мяу!

Барсик: мяу!

Барсик: мяу!

Кот мяукал 5 раз
***

3 Задание (Задача 5)

Составить программу, которая удаляет из списка L все элементы с указанным значением.

Задача решается созданием класса ListRemover, в котором:

- Метод для удаления символа

Решение задачи:

- На вход дается список (Который заполняется с клавиатуры). Программа запрашивает какой символ удалить и он удаляется из списка

Вывод:

Сколько элементов добавить в список? 4

Введите элемент 1: ф

Введите элемент 2: ы

Введите элемент 3: в

Введите элемент 4: а

Исходный список: [ф, ы, в, а]

Введите значение для удаления: ы

Удалено элементов: 1

Список после удаления: [ф, в, а]
***

4 Задание (Задача 2)

На городской олимпиаде по информатике участникам было предложено выполнить 3 задания,
каждое из которых оценивалось по 25-балльной шкале. Известно, что общее количество
участников первого тура олимпиады не превосходит 250 человек. На вход программы подаются
сведения о результатах олимпиады. В первой строке вводится количество участников N. Далее
следуют N строк, имеющих следующий формат:
<Фамилия><Имя><Баллы>
Здесь<Фамилия>– строка, состоящая не более чем из 20 символов;<Имя>– строка, состоящая не
более чем из 15 символов;<Баллы>– строка, содержащая три целых числа, разделенных
пробелом, соответствующих баллам, полученным участником за каждое задание первого тура.
При этом <Фамилия> и <Имя>, <Имя> и <Баллы> разделены одним пробелом. Примеры входных
строк:
Петрова Ольга 25 18 16
Калиниченко Иван 14 19 15
Напишите программу, которая будет выводить на экран фамилию и имя участника, набравшего
максимальное количество баллов. Если среди остальных участников есть ученики, набравшие
такое же количество баллов, то их фамилии и имена также следует вывести. При этом имена и
фамилии можно выводить в произвольном порядке.

Задача решается созданием классов Olympiad и Participant

В Olympiad входит:

- 2 поля (Список map из участников (Количество баллов, участник) и максимальный результат)

- Геттеры и Сеттеры

- Метод для поиска победителя

- Метод для добавления участника

Класс Participant содержит:

- 2 поля для имени (Фамилия и имя)

- 4 поля для счета (3 результата и итоговая сумма)

- Конструктор

- Геттеры и сеттеры для каждого поля

Решение задачи:

- На вход подается файл из участников.

- Он считывается и программа делит всех участников (их количество подается первым числом) по имени, фамилии и иъх результатам.

- Создается новый участник и суммируются их баллы

- Находится лучший участник и выводится его результат

Пример вывода:

4 Задание

Максимальный балл: 60

• Иванов Иван - 60 баллов

Всего победителей: 1
***

5 Задание (Задача 3)

Файл содержит текст на русском языке. Напечатать в алфавитном порядке все согласные буквы, которые входят ровно в одно слово.

Для решения задачи создается класс FindLetters. Его содержимое:

- Метод для поиска согласных которые встречаются лишь однажды

- Метод для чтения файла

Решение:

- Считывается файл

- Происходит поиск тех согласных, которые употребляются лишь единожды

- Их массив выводится в алфавитном порядке

Пример вывода:

5 Задание

Чтение файла: b.txt

Согласные буквы, входящие ровно в одно слово:

в д к л м п т 

Всего найдено: 7
***

6 Задание (Задача 4)

Проверить равенство участка очереди с i-го по j-й элемент (i < j).

Для решения задачи создается класс QueueEquals, в котором:

- Метод для сравнения элементов очереди

Решение:

- С клавиатуры вводится очередь из элементов

- На ввод запрашивается 2 индекса между которыми будет сравнение

- С помощью метода из класса QueueEquals сравниваются элементы от первого до второго индекса

- Выводится ответ

Пример вывода:

6 Задание

Сколько элементов добавить в очередь? 5

Введите элементы очереди:

Элемент 1: 1

Элемент 2: 2

Элемент 3: 3

Элемент 4: 4

Элемент 5: 5

Очередь: [1, 2, 3, 4, 5]

Введите начальный индекс i: 3

Введите конечный индекс j: 5

Результат: false
***

7 Задание (Задача 1)

Необходимо написать стрим:
Дан набор объектов типа Point, необходимо взять все Point в разных координатах, убрать с одинаковыми X,Y, отсортировать по X, отрицательные Y сделать положительными и собрать это все в ломаную (объект типа Polyline)
Если раннее в вашем варианте не было задание с классом Point и Polyline, то написать их:

1. класс Point:
   
• Координата Х: число.

• Координата Y: число.

• Может возвращать текстовое представление вида “{X;Y}”.

2. класс Line (Линия), расположенная на двумерной плоскости, которая описывается:
   
• Координата начала: Точка

• Координата конца: Точка

• Может возвращать текстовое представление вида “Линия от {X1;Y1} до {X2;Y2}”

3. класс Polyline (Ломаная), которая будет представлять собой ломаную линию. Ломаная линия представляет собой набор следующих характеристик:
   
• Имеет массив Точек, через которые линия проходит.

• Может быть приведена к строковой форме вида “Линия [Т1,T2,…,TN]”, где TN – это результат приведения к строке Точки с номером N

Для решения задачи создается 4 класса: Point, Line, Polyline и PointSort

Содержимое Point:

- 2 поля (Координата x и y)

- Геттеры для полей

- Конструктор

- equals и hashCode

- toString

Содержимое Line:

- 2 поля (Начало и конец)

- Конструктор

- Геттеры

- toString

Содержимое Polyline:

- Поле (Список из точек)

- Конструктор

- Геттер

- toString

Содержимое PointSort:

- Метод для работы с точками

Решение задачи:

- С клавиатуры вводится несколько точек

- Удаляются дубликаты

- Сортируются по возрастанию X

- Координаты Y у этих точек преобразуются в положительные

- Собирается итоговый список

- Создается ломанаая линия

- Вывод результата

Пример вывода:

7 Задание

Сколько точек добавить? 3

Точка 1:

Введите координату X: 1

Введите координату Y: 1

Точка 2:

Введите координату X: -1

Введите координату Y: 1

Точка 3:

Введите координату X: -2

Введите координату Y: 0

Исходные точки:

{1.0;1.0}

{-1.0;1.0}

{-2.0;0.0}

Результирующая ломаная линия:

Линия [{-2.0;0.0},{-1.0;1.0},{1.0;1.0}]

***

8 Задание (Задача 1)

Дан текстовый файл со строками, содержащими имя человека и его номер в следующей форме:

Вася:5

Петя:3

Аня:5

Номера людей могут повторяться. У каких-то людей может не быть номера.
Необходимо написать стрим выполняющую следующее:
читаются все люди из файла, все имена приводится к нижнему регистру, но с первой буквой в
верхнем регистре, убираем из перечня всех людей без номеров, а имена оставшихся группируются
по их номеру:
[5:[Вася, Аня], 3:[Петя]]

Для выполнения задания создается класс StreamSort внутри которого

- Метод для выполнения задания

- Метод для формирования правильного имени

Решение:

- На вход поступает файл с номерами и именами

- Считываются все стрки

- Ввод делится на номер и имя

- Убираются люди без номеров

- Форматируем имя

- Проверяем на целое число номер

- Группируем по номеру имена

- Выводим на экран

Пример вывода:

Задание 8

Результат группировки:

3: [Петя]

5: [Вася, Аня]
***







